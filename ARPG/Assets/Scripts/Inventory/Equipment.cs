using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Equipment : MonoBehaviour
{/*
	//This is the amount of inventoy spaces
	public const int inventorySpace = 4;

	public Equipable armourSlot;

	public Equipable wandSlot;
	public Equipable swordSlot;


	public bool AddEquipment(Equipable itemEquip){
		if (itemEquip.equipType == Equipable.EquipType.Armour) {
			if (armourSlot != null) {
				armourSlot = itemEquip;
			} else {
				Player.inventory.AddItem (armourSlot);
				armourSlot = itemEquip;
			}

		} else if (itemEquip.equipType == Equipable.EquipType.Wand) {
			if (wandSlot != null) {
				wandSlot = itemEquip;
			} else {
				Player.inventory.AddItem (wandSlot);
				wandSlot = itemEquip;
			}

		} else if (itemEquip.equipType == Equipable.EquipType.Weapon) {
			if (swordSlot != null) {
				swordSlot = itemEquip;
			} else {
				Player.inventory.AddItem (swordSlot);
				swordSlot = itemEquip;
			}
		}

	}

	public bool RemoveEquipment(Equipable itemEquip){

		Player.inventory.AddItem (itemEquip);
	}


	*/



}
